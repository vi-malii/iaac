pipeline {
    agent any
   
    stages { 
        stage('Deploy') { 
            steps { 
                sh 'ansible-playbook ansible/playbook.yml -i ansible/host.ini -vvv'
            }
        }
    }
}